# Test Database Configuration (matches CI)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=testuser
DB_PASSWORD=testpass
DB_DATABASE=testdb

# JWT Configuration
JWT_SECRET=test-secret-key
JWT_EXPIRES_IN=1h

# Node Environment
NODE_ENV=test
